{
  "name": "guionardo-portal",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "npm run build:site",
    "preview": "astro preview",
    "build:resume": "node scripts/build-resume.mjs",
    "build:site": "astro build",
    "run": "npm run build"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.0",
    "@astrojs/sitemap": "^3.0.0",
    "astro": "^7.3.2"
  }
}
